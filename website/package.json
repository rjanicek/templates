{
  "name": "PROJECT-NAME",
  "private": "true",
  "version": "0.0.0",
  "devDependencies": {
    "browserify": "^6.2.0",
    "glob": "^4.0.6",
    "lodash": "^2.4.1",
    "nodeunit": "^0.9.0",
    "uglify-js": "^2.4.15"
  },
  "dependencies": {
    "brfs": "^1.2.0",
    "browserify-middleware": "^4.1.0",
    "compression": "^1.2.0",
    "express": "^4.10.1"
  },
  "scripts": {
    "build": "npm run build-browserify && npm run build-uglify",
    "build-browserify": "browserify -t brfs scripts/browser/index.js > public/index.js",
    "build-debug": "browserify -d -t brfs scripts/browser/index.js > public/index.js",
    "build-uglify": "uglifyjs public/index.js -m -c -o public/index.js",
    "start": "node index",
    "test": "node test"
  }
}
